## v.1.0.0

- Initial release targeting the first experimental run of the new IHP SG13CMOS5L process.

Note: the netlist has been omitted from the release intentionally, as the design is not LVS clean (or rather, the LVS scripts are incomplete) and adding the netlist enables automated LVS.
